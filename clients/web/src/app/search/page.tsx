import { ComingSoon } from '@/components/shared/coming-soon';

export default function SearchPage() {
    return (
        <ComingSoon
            title="Busca de produtos"
            description="Estamos finalizando a busca para facilitar a sua experiência de compra."
        />
    );
}